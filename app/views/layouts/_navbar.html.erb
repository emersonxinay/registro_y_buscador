<nav class="navbar navbar-expand-lg bg-dark">
  <div class="container">
    <a class="navbar-brand" href="#"><%= link_to "Portal Usuarios", usuarios_path, class: "btn btn-primary  me-auto px-3" %></a>
    

    <button class="navbar-toggler " type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse mx-4" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item ">
          <a class="nav-link active" aria-current="page" href="#"><%= link_to "Registra usuarios", root_path, class: "text-primary navbar-brand" %></a>
        </li>
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="#"><%= link_to "lista usuarios", usuarios_path, class: "text-primary navbar-brand" %></a>        </li>
        
        
      </ul>
      
      <!-- hacer busqueda -->
      <form class="d-flex" role="search">
        <%= form_with(url:'/listar_usuarios', method: :get) do |f| %>
          <%= f.text_field :busqueda, placeholder: "buscar", class: "form-control" %>
          <%= f.submit "Buscar", class: "btn btn-primary" %>
        <% end %>
      </form>
    </div>
  </div>
</nav>